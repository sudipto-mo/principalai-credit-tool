.env
.env.local
.env.*.local
.vercel
node_modules
.next
.tmp_pypdf/
test-results/
tsconfig.tsbuildinfo

# Universe Map pipeline — regenerated artifacts, not source

# Stage-1 history snapshots — append-only, regenerated on each run.
# Rolled up into DuckDB once score engine ships.
